# AI30.7 — Scheduling Catalog

Integrated into existing Catalog (`SetupHub`) → Scheduling hub.  
Routes under `/setup/scheduling/*`. Reuses MainLayout; responsive MUI.  
Permissions: `Scheduling.View` / `Scheduling.Manage`.
