# AI30 Phase 2B.5.4 — Configurable Rule Thresholds

Detection rules unchanged. Values moved to `ConflictRules` appsettings + `SchedulingConflictRuleThresholdSetting` DB override with audit history.

Admin UI: Scheduling → Conflict Rule Thresholds.
