# Operational Intelligence Guide

Widgets are defined in `DashboardWidgetCatalog` and rendered by `DashboardWidgetGrid`.

Configurable per user via `DashboardPreference` (hide / reorder / compact / landing page).

Analytics compose Recovery, Enterprise Ops, Department Operations, and Scheduling Readiness — no duplicate SQL.
