# AI11.2.H7 – Value Object Review

## Value object

`ClassroomImageMetadata` in `Abhyanvaya.Domain/ValueObjects/`

| Field | Former column |
|-------|---------------|
| `ImageKey` | `OriginalImageKey` |
| `ImageHash` | `OriginalImageHash` |
| `Width` | `ImageWidth` |
| `Height` | `ImageHeight` |
| `Orientation` | `ImageOrientation` |
| `CaptureTimestamp` | `CaptureTimestamp` |
| `CaptureDevice` | `CaptureDevice` |
| `UploadedUtc` | `ImageUploadedUtc` (new column) |

## Aggregate exposure

`AttendanceSession.ImageMetadata` – owned entity via EF Core `OwnsOne`.

## Domain methods

| Method | Purpose |
|--------|---------|
| `AttachClassroomImage()` | Sets metadata, thumbnail key, filename, method |
| `SetImageDimensions()` | Updates width/height after AI detection |

## Remaining session fields

| Field | Reason |
|-------|--------|
| `ThumbnailImageKey` | Separate storage variant |
| `AnnotatedImageKey` | Post-processing overlay |
| `OriginalFileName` | Client filename (not in value object spec) |

## EF mapping

`AttendanceSessionConfiguration` maps owned entity columns to existing table columns (no table split).

## API / UI

No DTO or UI changes – `AttendanceSessionQueryService` reads from `ImageMetadata`.

**Status: Complete**
