# Admin Enterprise Operations Dashboard Guide

Navigate to **Dashboard** (Admin) or `/dashboard/admin`.

Sections: Academic, Attendance, Scheduling, Faculty, Student, Recovery, AI Services, Platform Health.

Related surfaces:

- `/dashboard/analytics` — trends + department comparison + export
- `/dashboard/notifications` — enterprise inbox
- `/dashboard/health` — traffic lights
- `/setup/attendance-recovery` — deep recovery ops (unchanged)
