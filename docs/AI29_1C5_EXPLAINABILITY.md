# AI29.1C.5 — Explainability

```mermaid
flowchart TD
  Trace[Strategy Trace] --> Expl[Explanation]
  Cons[Constraint Trace] --> Expl
  Score[Score Breakdown] --> Expl
```

Deterministic only — no LLMs. Reasons come from recommendation explanations + constraint evaluations.
