# AI20.1 Schema Compatibility

## Required Objects

| Table | Critical Columns |
|-------|------------------|
| `StudentEnrollmentBatch` | `ConfigurationSnapshotJson`, `CorrelationId`, `PhotoProviderName`, `PipelineVersion`, `Priority` |
| `StudentEnrollmentProgressSnapshot` | (table) |
| `ArtifactStorageManifest` | (table) |
| `ArtifactRegistryEntry` | (table) |

## Compatibility Matrix

| Environment | Expected Mode |
|-------------|---------------|
| Clean CI database | Upgrade |
| New install | FreshInstall → migrate → Upgrade |
| Partial manual patch | PartialSchema → run fix migration |
| Missing enrollment tables | LegacyDatabase |

## Diagnostics

Run:

```csharp
var report = await migrationValidator.ValidateAsync();
```

Report includes `SchemaVersion` (latest applied migration id).
