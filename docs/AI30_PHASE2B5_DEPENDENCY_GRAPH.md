# AI30 Phase 2B.5.3 — Conflict Dependency Graph

`ConflictDependencyAnalyzer` produces nodes/edges, clusters, and Mermaid (`flowchart TD`). Workspace dialog supports expand/collapse via cluster lists and Open cell navigation. No optimization.
