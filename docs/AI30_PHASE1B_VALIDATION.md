# AI30.6B — Validation Framework

FluentValidation for Faculty Preferences, Room Features, Subject Delivery, Holiday Types.

Rules: required refs, priorities, no duplicate active preferences, no duplicate feature assignments, delivery↔room type mapping.

**Report:** `GET api/scheduling/validation-report` — metadata only (no scheduling/optimizer).
