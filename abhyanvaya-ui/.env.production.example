# Copy to .env.production before a production build, or set in Cloudflare Pages / Netlify / Vercel UI.
# Must match deployed API base URL including /api suffix.
VITE_API_BASE_URL=https://your-api.onrender.com/api
