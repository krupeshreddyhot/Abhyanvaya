# Vite proxies /api, /media, and /hubs here (vite.config.ts).
# Local API is currently listening on http://localhost:5000 (confirm with netstat if images 502).
VITE_API_PROXY_TARGET=http://localhost:5000
